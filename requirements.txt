# Detox Backend Dependencies
# Note: torch is installed separately in Dockerfile (CPU-only version)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
easyocr>=1.7.0
pillow>=10.0.0
python-multipart>=0.0.6
numpy>=1.24.0
pydantic>=2.0.0
